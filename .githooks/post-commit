#!/bin/bash
# post-commit hook — auto-bump version after every commit
# public/VERSION + public/js/version.js + package.json are staged for the next commit
# Skip with: git commit --no-verify

scripts/version-bump.sh
git add public/VERSION public/js/version.js package.json 2>/dev/null || true